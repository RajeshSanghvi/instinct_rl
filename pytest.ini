[pytest]
testpaths = tests
# tests/ has no __init__.py on purpose: pytest's default "prepend" import mode then puts
# tests/ itself on sys.path, which is what makes `from helpers import ...` resolve.
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
